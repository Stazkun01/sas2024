#include<stdio.h>
int main(){
        int list[5];
        int temp;
        for (temp =0; temp < 5 ; temp++){
           printf("Entrer un entier:");
           scanf("%d",&list);
        }
}